/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,ts}"], // templates Angular (externes et inline)
  darkMode: "class",                  // optionnel, utile si tu veux un thème sombre
  theme: { extend: {} },
  plugins: [],
};
